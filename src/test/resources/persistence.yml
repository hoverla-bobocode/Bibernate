logLevel: INFO

persistenceUnit:
  name: h2
  dataSource:
    jdbcUrl: jdbc:h2:mem:testdb
    user: sa
    password: password
  dialect: h2

---
persistenceUnit:
  name: unused-unit
  dataSource:
    jdbcUrl: unused-url
    user: root
    password: root

